def solution(arrows):
    answer = 0
    
    # 0번부터 7번까지 8방향 이동 설정 (위쪽부터 시계방향)
    dx = [0, 1, 1, 1, 0, -1, -1, -1]
    dy = [1, 1, 0, -1, -1, -1, 0, 1]
    
    # 사이클(방)이 생기는지 확인하기 위해 방문한 정점과 간선을 기록하려고 시도함
    visited_nodes = set()
    visited_edges = set() # 간선 중복 체크용
    
    # 시작점 초기화
    x, y = 0, 0
    visited_nodes.add((x, y))
    
    for arrow in arrows:
        # 다음 이동할 좌표 계산
        nx = x + dx[arrow]
        ny = y + dy[arrow]
        
        # [시도 및 한계점]
        # 아이디어: 이미 방문했던 노드(visited_nodes)에 다시 도착했는데, 
        # 처음 지나가는 간선(visited_edges)이라면 방이 하나 만들어진다고 생각함.
        
        # 문제 1. 대각선 이동끼리 서로 교차하면서 'X'자 모양이 될 때, 
        # 정점이 생기지 않음에도 방이 2개로 쪼개지는 예외 상황을 발견함.
        # 이를 해결하려면 좌표를 2배로 늘려서(스케일업) 교차점을 정점으로 만들어야 한다고 
        # 힌트를 보았으나, 구현 코드가 너무 복잡해져서 실패함.
        
        # 문제 2. 간선을 기록할 때 (A->B)와 (B->A)를 같은 간선으로 취급해서 
        # visited_edges에 넣어야 하는데 이 부분 로직을 짜다가 꼬임.
